import { LoginForm } from "@/components/LoginForm"

const LoginPage = () => {
  return (
    <main>
      <section>
        <LoginForm />
      </section>
      <section>
      </section>
    </main>
  )
}

export default LoginPage;